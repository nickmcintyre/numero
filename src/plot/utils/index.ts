import {
  min,
  max,
  range,
} from './data';
import { defaultProps, Props, Range } from './props';
import inferTypes from './table';

export {
  min,
  max,
  range,
  Range,
  defaultProps,
  Props,
  inferTypes,
};
